# Copy to .env (gitignored) for live read-only mode. Mock mode needs NONE of these.
# Never commit real tokens. Account numbers and tokens are never logged.
SCHWAB_ACCESS_TOKEN=
SCHWAB_REFRESH_TOKEN=
SCHWAB_TOKEN_EXPIRES_AT=0
# Optional: override the config directory.
# PORTFOLIO_TOOL_CONFIG=./config
